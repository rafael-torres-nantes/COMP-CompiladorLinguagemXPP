// Teste completo do analisador léxico

/* Este é um comentário 
   de bloco que pode ter
   múltiplas linhas */

class MinhaClasse extends OutraClasse {
    int numero;
    string texto;
    
    constructor(int n, string t) {
        numero = n;
        texto = t;
    }
    
    int calcular(int a, int b) {
        int resultado;
        resultado = a + b - 10;
        resultado = resultado * 2 / 3;
        resultado = resultado % 5;
        
        if (a < b) {
            return a;
        } else if (a > b) {
            return b;
        } else if (a <= b) {
            return 0;
        } else if (a >= b) {
            return 1;
        } else if (a == b) {
            return 2;
        } else if (a != b) {
            return 3;
        }
        
        return resultado;
    }
    
    print mensagem() {
        print("Hello World");
        print(numero);
    }
}

// Programa principal
int main() {
    int x;
    int y;
    int z[10];
    string nome;
    
    read(x);
    read(y);
    
    for (int i = 0; i < 10; i = i + 1) {
        z[i] = i * 2;
    }
    
    MinhaClasse obj;
    obj = new MinhaClasse(100, "teste");
    
    if (x != y) {
        print("diferentes");
    }
    
    return 0;
}

// Teste de identificadores e números
_underscore
var123
VAR_ABC
a1b2c3
_123abc
12345
0
999